# 外部定时器配置说明（替代 GitHub 自带 schedule）

## 背景
GitHub 自带的 `schedule` 定时器在本仓库实测始终不触发（历史 23 次运行全部为 `workflow_dispatch` 手动触发，0 次 schedule）。
但 `workflow_dispatch` 手动/API 触发通道完全正常（全部 success，已实跑验证）。

因此改为：**用外部免费定时器（cron-job.org）定时调用 GitHub 的 workflow_dispatch 接口**，
完全绕过 GitHub 坏掉的 schedule 调度器，实现"关机也能自动提醒/导出"。

## 你需要的东西
1. 一个 GitHub Fine-grained PAT，**必须带 `actions:write` 权限**（外部服务用它调用 dispatch API）。
   - 生成：GitHub → Settings → Developer settings → Fine-grained tokens → New token
   - Repository access → Only select repositories → 勾 `huangyuxuan11/feishu-log-reminder`
   - Permissions → Repository permissions → **Actions = Read and write**
   - 过期建议设 90 天或更长
2. 一个 cron-job.org 免费账号（https://cron-job.org ，无需信用卡）

## 在 cron-job.org 配置（每个时间点建一个 job）

### 通用设置
- **Method**: `POST`
- **Request headers**（逐条添加）:
  - `Authorization: Bearer <你的PAT>`
  - `Accept: application/vnd.github+json`
  - `Content-Type: application/json`
- **Request body** (raw):
  ```json
  {"ref":"main"}
  ```
- **Schedule** 用 Advanced(cron)，cron-job.org 按 **UTC** 计算：
  - 格式：`分 时 日 月 周`，周 `1-5` = 周一至周五

### 四个 job 一览
| 名称 | 工作流 ID | URL（拼接用） | UTC cron | 北京时间 |
|---|---|---|---|---|
| 提醒-20:00 | 321885974 | `https://api.github.com/repos/huangyuxuan11/feishu-log-reminder/actions/workflows/321885974/dispatches` | `0 12 * * 1-5` | 20:00 |
| 提醒-20:30 | 321885974 | 同上 | `30 12 * * 1-5` | 20:30 |
| 提醒-20:45 | 321885974 | 同上 | `45 12 * * 1-5` | 20:45 |
| 导出-21:30 | 321922123 | `https://api.github.com/repos/huangyuxuan11/feishu-log-reminder/actions/workflows/321922123/dispatches` | `30 13 * * 1-5` | 21:30 |

> 即：把上表「URL」填进 cron-job.org 的 URL 框，Method 选 POST，加好三个 header，body 填 `{"ref":"main"}`，Schedule 填对应的 UTC cron 即可。

## 验证
配置后，在 cron-job.org 每个 job 点 "Run now" 立即测试一次，观察：
- GitHub Actions 出现新的 `workflow_dispatch` 运行且 success
- 你飞书收到对应私信（提醒名单 / 导出 Excel 或"📭 暂无填报"）

> 导出任务需要飞书应用有 `im:resource` 权限才能发文件；实测 2026-07-29 当日导出已 success（说明权限足够）。若日后改动后报文件上传类错误，去开放平台补 `im:resource` 并发布新版本。
