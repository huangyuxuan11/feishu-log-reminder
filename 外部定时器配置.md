# 定时方案说明（GitHub 自带 schedule + 飞书自动化 双保险，去重防重复）

## 背景
本仓库采用**两套互相独立的定时器**，同一时间档位（北京 20:00 / 20:30 / 20:45 提醒，21:30 导出，22:00 报告）都会点火，
任一可用即可保证"关机也能自动提醒/导出/出报告"：

1. **飞书多维表格自动化（云端定时器）**：定时调用 GitHub 的 `workflow_dispatch` 接口触发工作流。
2. **GitHub 自带 `schedule` 定时器**：直接由 GitHub 调度器触发同一工作流（已于 2026-07-29 重新加回尝试）。

> 历史曾出现 GitHub 自带 schedule 不触发（23 次运行 0 次 schedule）；现已重新启用观察。
> 无论哪套生效，脚本侧有**去重控制表**保证同一档位当天只发一次，绝不重复打扰。

## 去重机制（关键）
- 飞书新建控制表 **「日志提醒去重控制」** `H0qGbDCJiaMXQJss7xOcvTYEnsh`（表 `tbleEVzbXaicZ19E`）。
- 每个脚本运行时会先按当前时间判定档位（2000/2030/2045/2130/2200），查控制表：本档位今日已发过 → 直接跳过。
  - 报告脚本档位键为 `2200`，去重任务名 `report`（与 remind/export 互不干扰）。
- 发送成功后把档位写回控制表。两路触发器（飞书 / GitHub）谁先发谁写，后者检测到已发即跳过。
- **fail-open**：控制表读写出错也照常发送（宁可偶尔重复，绝不漏报告）。

## 当前生效配置（2026-07-29 部署）

承载自动化的多维表格：**「GitHub定时触发器（日志提醒）」**
https://tcnq95mojezp.feishu.cn/base/ABgGbDCJiaMXQJss7xOcvTYEnsh
（打开后点右上角「自动化」图标即可查看/编辑这些流程）

| 自动化 | workflow_id | 定时规则 | 触发的 GitHub 工作流 |
|---|---|---|---|
| 工作日20:00 触发GitHub-未填提醒 | wkffVtuCv208p5fd | 工作日 20:00 | 未填提醒 (321885974) |
| 工作日20:30 触发GitHub-未填提醒 | wkfDR7X7S3TkEhS6 | 工作日 20:30 | 未填提醒 (321885974) |
| 工作日20:45 触发GitHub-未填提醒 | wkfX9erjnY9zIrRI | 工作日 20:45 | 未填提醒 (321885974) |
| 工作日21:30 触发GitHub-每日导出 | wkfftNaeyiMK9D98 | 工作日 21:30 | 每日导出 (321922123) |
| 工作日22:00 触发GitHub-每日报告 | （待在飞书侧新增，见下） | 工作日 22:00 | 每日报告（report.yml） |

每条自动化结构：`TimerTrigger(WORKDAY)` → `HTTPClientAction`：
- POST `https://api.github.com/repos/huangyuxuan11/feishu-log-reminder/actions/workflows/<ID>/dispatches`
- Headers: `Authorization: Bearer <PAT>`、`Accept: application/vnd.github+json`、`Content-Type: application/json`
- Body: `{"ref":"main"}`

GitHub 工作流 `schedule`（UTC，已加回）：
- 未填提醒：`0,30,45 12 * * 1-5`（=北京 20:00/20:30/20:45）
- 每日导出：`30 13 * * 1-5`（=北京 21:30）
- 每日报告：`0 14 * * 1-5`（=北京 22:00）

## PAT 说明
飞书自动化里内嵌了一个 GitHub Fine-grained PAT（仅授权本仓库，Actions=Read and write）。
**PAT 过期后需更新**：GitHub 重新生成同权限 PAT → 打开上面的多维表格 → 自动化 → 逐条编辑 HTTP 步骤的 Authorization 头。
- 生成路径：GitHub → Settings → Developer settings → Fine-grained tokens → New token
- Repository access → Only select repositories → 勾 `huangyuxuan11/feishu-log-reminder`
- Permissions → Repository permissions → **Actions = Read and write**

## 验证
- GitHub Actions 出现新的 `workflow_dispatch` / `schedule` 运行且 success
- 飞书收到对应私信（提醒名单 / 导出 Excel 或"📭 暂无填报"），且同一档位当天仅一条

> 导出任务需要飞书应用有 `im:resource` 权限才能发文件；实测 2026-07-29 当日导出已 success（说明权限足够）。若日后改动后报文件上传类错误，去开放平台补 `im:resource` 并发布新版本。

## 备选方案（如两套都不可用）
也可用 cron-job.org 等外部定时器，POST 同样的 dispatch 接口即可：
UTC cron `0/30/45 12 * * 1-5`（=北京 20:00/20:30/20:45）、`30 13 * * 1-5`（=北京 21:30）。
